// Placeholder route: the skill replaces it with the cloned page.
export default function Home() {
  return (
    <main className="grid min-h-screen place-items-center p-8">
      <p className="text-center text-muted-foreground">
        Nothing cloned yet. Start with{" "}
        <code className="font-mono text-foreground">/website-cloner &lt;url&gt;</code>.
      </p>
    </main>
  );
}
